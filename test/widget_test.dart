void main() {
  // Widget tests are skipped due to heavy reliance on native macOS window APIs
  // (window_manager) which cannot be mocked in the test environment.
  // Business logic can be unit tested via timer_state.dart directly.
}
